container:
  image: quay.io/droidian/build-essential:bookworm-amd64
  cpu: 8
  memory: 32G
env:
  GITHUB_TOKEN: ENCRYPTED[b28f79174c63e23e95a6767888d6b64b4c377572a2a4ee842cc115cce0e2d9f4dc6a655053bea7928630ae64dbefffee]
  PACKAGES_DIR: "$HOME/packages"
  KERNEL_DIR: "$HOME/android_kernel_xiaomi_rosy"
  FILES_DIR: "$HOME/halium_device_xiaomi_rosy"

build_task:
  preparation_script:
    - echo $PWD
    - cd ~/
    - echo $PWD
    - git clone https://github.com/neizhmak/halium_device_xiaomi_rosy.git
    - mkdir -p packages/buildd
    - git clone https://github.com/baunilla/android_kernel_xiaomi_rosy.git
    - cd $KERNEL_DIR
    - git checkout -b bookworm
    - cp $FILES_DIR/rosy-perf-halium_defconfig arch/arm64/configs/
    - mkdir -p buildd/sources/debian/source
    - apt update -y
    - apt install -y linux-packaging-snippets
    - cd buildd/sources/debian
    - cp $FILES_DIR/kernel-info.mk ./
    - echo 13 >> compat
    - echo "3.0 (native)" >> source/format
    - cp $FILES_DIR/rules ./
    - chmod +x rules
  build_script:
    - cd $KERNEL_DIR/buildd/sources
    - debian/rules debian/control
    - RELENG_HOST_ARCH="arm64" releng-build-package
  publish_release_script: source $FILES_DIR/publish_release.sh
